const employee = require("../lib/employee");

describe("employee", () => {
  describe("Initialization", () => {
    it(
      "should create an object with a 'name' property set to the user input in the terminal"
    ),
      () => {
        //arrange
        const person = new Employee();

        //act
        //assert
      };
  });
  //other two inputs
});
